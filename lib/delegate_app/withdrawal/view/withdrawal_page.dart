import 'package:flutter/material.dart';

class WithdrawalPage extends StatelessWidget {
  static Page page() => const MaterialPage(child: WithdrawalPage());
  const WithdrawalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
